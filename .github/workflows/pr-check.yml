name: PR Check

on:
  pull_request:
    types: [opened, synchronize, reopened]

# Cancel in-flight runs for the same PR on new pushes
concurrency:
  group: pr-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  pr-validate:
    name: Validate PR
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check PR title format
        # Title through env, never inline ${{ }} — same injection vector as the
        # description check below ($(…) in a PR title would execute here).
        env:
          PR_TITLE: ${{ github.event.pull_request.title }}
        run: |
          # Conventional Commits: <type>[optional scope][optional !]: <description>
          # The scope and the breaking-change "!" are part of the spec and are used
          # throughout this repo's own history (fix(ios):, chore(deps):,
          # test(integration): …) — the previous pattern rejected all of them, so a PR
          # titled the same way as the commits it contains could not pass.
          if ! echo "$PR_TITLE" | grep -qE '^(feat|fix|docs|test|refactor|chore|ci|perf|style)(\([a-z0-9._/-]+\))?!?: .+'; then
            echo "❌ Invalid PR title format!"
            echo ""
            echo "PR title must follow conventional commits format:"
            echo "  feat: Add new feature"
            echo "  fix: Fix bug"
            echo "  docs: Update documentation"
            echo "  test: Add tests"
            echo "  refactor: Refactor code"
            echo "  chore: Maintenance tasks"
            echo ""
            echo "An optional scope and breaking-change marker are also accepted:"
            echo "  fix(ios): Fix an iOS-only bug"
            echo "  feat(android)!: Add a feature with a breaking change"
            echo "  ci: CI/CD changes"
            echo "  perf: Performance improvements"
            echo "  style: Code style changes"
            echo ""
            echo "Current title: $PR_TITLE"
            exit 1
          fi
          echo "✅ PR title format is valid"

      - name: Check for description
        # PR body goes through env, NEVER inline ${{ }} into the script: inlined
        # bodies both break the shell on quotes/parens and allow command
        # injection ($(…) in a PR description would execute on the runner).
        env:
          PR_BODY: ${{ github.event.pull_request.body }}
        run: |
          BODY_LENGTH=$(printf '%s' "$PR_BODY" | wc -c)
          if [ "$BODY_LENGTH" -lt 20 ]; then
            echo "⚠️ Warning: PR description is very short or empty"
            echo "Consider adding more context about the changes"
          else
            echo "✅ PR has a description"
          fi

      - name: Check for large PR
        run: |
          FILES_CHANGED=${{ github.event.pull_request.changed_files }}
          if [ "$FILES_CHANGED" -gt 50 ]; then
            echo "⚠️ Warning: This PR modifies $FILES_CHANGED files"
            echo "Consider breaking it into smaller PRs for easier review"
          else
            echo "✅ PR size is reasonable ($FILES_CHANGED files)"
          fi

  quick-check:
    name: Quick Lint & Format
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version-file: .flutter-version
          channel: stable
          cache: true

      - name: Get dependencies (plugin)
        run: flutter pub get

      - name: Get dependencies (gen)
        working-directory: native_workmanager_gen
        run: dart pub get

      - name: Check formatting (plugin)
        run: |
          dart format --output=none --set-exit-if-changed lib test || {
            echo "❌ Run: dart format lib test"
            exit 1
          }
          echo "✅ Plugin formatted"

      - name: Check formatting (gen)
        working-directory: native_workmanager_gen
        run: |
          dart format --output=none --set-exit-if-changed lib || {
            echo "❌ Run: dart format lib (in native_workmanager_gen/)"
            exit 1
          }
          echo "✅ Gen package formatted"

      - name: Analyze (plugin)
        run: flutter analyze --fatal-warnings

      - name: Analyze (gen)
        working-directory: native_workmanager_gen
        run: dart analyze --fatal-warnings

  label-check:
    name: Check Labels
    runs-on: ubuntu-latest
    if: github.event.action == 'opened'

    steps:
      - name: Suggest labels
        run: |
          PR_TITLE="${{ github.event.pull_request.title }}"

          echo "Suggested labels based on PR title:"
          if echo "$PR_TITLE" | grep -q '^feat:'; then
            echo "  - enhancement"
          elif echo "$PR_TITLE" | grep -q '^fix:'; then
            echo "  - bug"
          elif echo "$PR_TITLE" | grep -q '^docs:'; then
            echo "  - documentation"
          elif echo "$PR_TITLE" | grep -q '^test:'; then
            echo "  - tests"
          elif echo "$PR_TITLE" | grep -q '^refactor:'; then
            echo "  - refactoring"
          elif echo "$PR_TITLE" | grep -q '^perf:'; then
            echo "  - performance"
          fi

          echo ""
          echo "Please add appropriate labels to this PR for better organization."
